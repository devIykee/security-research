//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::EpochState;
use crate::generated::types::Winner;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct EpochVaultIteration {
pub discriminator: [u8; 8],
pub version: u16,
pub bump: u8,
pub iteration_id: u32,
pub state: EpochState,
pub total_tickets: u64,
pub participants_count: u32,
pub page_count: u16,
pub current_page_index: u16,
pub current_page_entry_count: u8,
pub entropy: [u8; 32],
pub claimable_usd: u64,
pub claimable_btc: u64,
pub total_tickets_remaining: u64,
pub current_winning_ticket: u64,
/// Pages sealed so far; selection may only start once every page is sealed.
pub sealed_pages_count: u16,
pub winners_selected: u8,
pub winners_claimed: u8,
pub winners: [Winner; 21],
/// Reserved.
pub reserved: [u8; 32],
}


pub const EPOCH_VAULT_ITERATION_DISCRIMINATOR: [u8; 8] = [116, 108, 198, 176, 193, 190, 91, 125];

impl EpochVaultIteration {
      pub const LEN: usize = 1036;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for EpochVaultIteration {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_epoch_vault_iteration(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<EpochVaultIteration>, std::io::Error> {
  let accounts = fetch_all_epoch_vault_iteration(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_epoch_vault_iteration(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<EpochVaultIteration>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<EpochVaultIteration>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::other(format!("Account not found: {address}")))?;
      let data = EpochVaultIteration::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_epoch_vault_iteration(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<EpochVaultIteration>, std::io::Error> {
    let accounts = fetch_all_maybe_epoch_vault_iteration(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_epoch_vault_iteration(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<EpochVaultIteration>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<EpochVaultIteration>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = EpochVaultIteration::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for EpochVaultIteration {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for EpochVaultIteration {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for EpochVaultIteration {
      fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(
          crate::SATRUSH_ID.to_bytes()
        )
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for EpochVaultIteration {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for EpochVaultIteration {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

