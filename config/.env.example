# Copy to repo root as .env and fill in values. Never commit .env.
# RPC endpoints
ETH_RPC_URL=
BASE_RPC_URL=
ARB_RPC_URL=

# Optional explorers / APIs
ETHERSCAN_API_KEY=
BASESCAN_API_KEY=

# Private keys / wallets — local testing only, never real funds in shared envs
PRIVATE_KEY=
